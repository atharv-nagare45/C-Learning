#include <stdio.h>

int main()
{
    if (1)
    {
        printf("this is proceed bcuz value in if bracket is non zero\n");
    }
    if (5.5)
    {
        printf("Same as above\n");
    }
    if (0)
    {
        printf("This value is not proceed bcuz of zero\n");
    }    return 0;
}